// Shift

// remove items from the beginning of the array

const array = [ 1, 2, 3, 4, 5]
const remove = array.shift();  // receives the value taken from the array

console.log(remove)
console.log(array)