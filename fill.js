// fill

// this method fills specified elements in an array with a value

// array.fill(value, start, end)

// easy example

const names = ["Daniel", "Julia", "Felipe", "Fulano"]

const result = names.fill("Max", 1, 2)

console.log(result)